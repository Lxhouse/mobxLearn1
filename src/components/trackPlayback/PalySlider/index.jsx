import { Slider } from "antd";

/**播放条 */
const PlaySlider = () => {
  return (
    <Slider
      className="pro"
      //   value={playbackProgress}
      max={200}
      //   onChange={progressSlider}
    />
  );
};
export default PlaySlider;
